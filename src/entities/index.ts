import { Credential } from "./credential.entity";
import { Identifier } from "./identifier.entity";
import { TrustScoreCredential } from "./trust-score.entity";

export const entities = [Identifier, Credential, TrustScoreCredential];